// 1 - Crie um programa que dado um número imprima a sua tabuada. 

const inserirNumero = 2;

for (let i = 1; i <= 10; i++) {
    const resultado = i*inserirNumero; 
    console.log(`${i} x ${inserirNumero} = ${resultado}`)
};
